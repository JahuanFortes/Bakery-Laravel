{{--@props(['filterBtn'])--}}
{{--@csrf--}}
{{--<form action="{{ url()->current() }}" method="GET" class="inline-block">--}}
{{--    <label for="filter" class="sr-only">Filter</label>--}}

{{--    <select name="category" id="filter" onchange="this.form.submit()"--}}
{{--            class="px-4 py-2 bg-blue-500 text-white font-semibold rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">--}}
{{--        <option value="" disabled selected>Select Filter</option>--}}

{{--        @foreach($allCategories as $category)--}}
{{--            <option value="{{$category->id}}">{{$category->title}}</option>--}}
{{--        @endforeach--}}
{{--    </select>--}}

{{--</form>--}}

